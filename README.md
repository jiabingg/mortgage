# Mortgage App (Node + Express)

A minimal mortgage calculator with the calculation logic on a Node.js backend. Frontend is a small static page that calls the API.

## Run locally

```bash
# 1) Install deps
npm install

# 2) Start dev (Node 18+)
npm run dev
# or
npm start

# 3) Open
# http://localhost:3000
